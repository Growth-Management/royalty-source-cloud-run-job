name: Deploy Cloud Run Job

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - fix/quality-check-samples

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-cloud-run-job-${{ github.ref }}
  cancel-in-progress: false

env:
  GCP_PROJECT_ID: ice